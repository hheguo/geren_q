.container {
  padding: 24rpx 32rpx;
  min-height: 100vh;
}

.room-code-input {
  text-align: center;
  font-size: 48rpx;
  font-weight: 700;
  letter-spacing: 16rpx;
  padding: 28rpx 32rpx;
  padding-right: 16rpx;
}

/* 按钮禁用态 */
.btn-disabled {
  opacity: 0.6;
  pointer-events: none;
}

/* ---------- 加载遮罩 ---------- */
.loading-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.55);
  z-index: 999;
  display: flex;
  align-items: center;
  justify-content: center;
}

.loading-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 24rpx;
}

.loading-spinner {
  width: 64rpx;
  height: 64rpx;
  border: 5rpx solid rgba(255, 255, 255, 0.15);
  border-top: 5rpx solid #818cf8;
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

.loading-text {
  font-size: 28rpx;
  color: #e2e8f0;
  font-weight: 500;
}
